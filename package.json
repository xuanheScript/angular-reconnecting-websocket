{
  "name": "reconnecting-websocket",
  "description": "A reconnecting websocket.",
  "author": "Joe Walnes",
  "scripts": {
    "build": "echo '// https://github.com/abachman/reconnecting-websocket' > reconnecting-websocket-min.js ; uglifyjs reconnecting-websocket.js -m >> reconnecting-websocket.min.js"
  },
  "devDependencies": {
    "uglify-js": "~2.4.4"
  }
}
